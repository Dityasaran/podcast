services:
  backend:
    build: .
    ports:
      - "5050:5050"
    volumes:
      - .env:/app/.env
    # The backend container needs to access its own environment variables
    env_file:
      - .env

  frontend:
    build: ./web
    ports:
      - "8080:8080"
    depends_on:
      - backend
